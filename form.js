$(document).ready(() => {
  const display = $('#display');
  const form = $('#form');
  const todoUserInput = $('#todoUserInput');

  const resetTodosInput = () => {
    todoUserInput.val('');
  };
});
